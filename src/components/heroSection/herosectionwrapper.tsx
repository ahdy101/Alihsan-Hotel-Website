import HeroSection from './herosection';

const HeroSectionWrapper = () => {
  // This can fetch data or handle other server-side logic
  return <HeroSection />;
};

export default HeroSectionWrapper;